{
  "name": "swarup-stationery-store",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "test": "npm test --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=20"
  }
}